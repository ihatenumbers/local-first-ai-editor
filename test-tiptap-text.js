const { Editor } = require('@tiptap/core');
const StarterKit = require('@tiptap/starter-kit');

// I cannot run this without dom environment easily. But what if we just check the user's text for weird characters?
